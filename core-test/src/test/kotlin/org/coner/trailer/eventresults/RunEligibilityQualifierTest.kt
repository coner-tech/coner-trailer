package org.coner.trailer.eventresults

import org.junit.jupiter.api.Test

class RunEligibilityQualifierTest {

    @Test
    fun `When all participant cf runs consume and size equals runCount, it should map all to core`() {
        TODO()
    }

    @Test
    fun `When all but one participant cf runs consume and size equals runCount, it should map all but runs that consume`() {
        TODO()
    }

    @Test
    fun `When no participant cf runs consume and size equals runCount, it should map none`() {
        TODO()
    }

    @Test
    fun `When all participant cf runs consume and size exceeds runCount, it should map up to runCount`() {
        TODO()
    }

    @Test
    fun `When a participant cf runs doesn't consume `() {

    }
}