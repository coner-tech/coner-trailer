package org.coner.trailer.cli.command.event

import com.github.ajalt.clikt.core.CliktCommand

class EventResultsCommand : CliktCommand(
    name = "results",
    help = "Event Results"
) {

    override fun run() = Unit
}