package tech.coner.trailer.io.service

import tech.coner.trailer.Event
import tech.coner.trailer.EventContext

class EventContextService {

    fun load(event: Event): Result<EventContext> {
        TODO()
    }
}