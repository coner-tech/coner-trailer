package tech.coner.trailer.presentation.model

import tech.coner.trailer.EventContext
import tech.coner.trailer.presentation.library.model.Model

class EventContextModel(
    val eventContext: EventContext,
) : Model {
}